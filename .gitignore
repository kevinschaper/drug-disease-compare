# Observable Framework
/dist/
**/.observablehq/cache/
node_modules/

# Python
__pycache__/
*.pyc
.venv/
.ruff_cache/
.pytest_cache/

# Large pinned inputs (re-fetch with `just fetch`); checksums pinned in data/MANIFEST.yaml
/data/inputs/

# Node Normalizer response cache (local build aid; rebuild with `just normalize`)
/data/nodenorm_cache.json

# Drug-group lookup cache (GSRS/RxNav/OLS; rebuilt on demand during `build`)
/data/drug_groups_cache.json

# Experiment run outputs (regenerable via experiments/drug_collapse.py)
/experiments/out/
